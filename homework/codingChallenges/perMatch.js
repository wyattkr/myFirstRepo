var lastname1 = "wyatt"
var lastname2 = "taborn"

function perMatched(name1, name2){
  // compare the names 
  // if any letters matches---
  // increment a number variable
  //if there are letters that don't match
  // increment a second number
  //divide the two numbers and return 
  // a score..... we already did it
  // look a codingChallengeTen.js
 for(var i = 0; i < perMatched.length; i++){
    if(perMatched[i] === toString){
        console.log("hey that is a wonderful name");
        result.push(perMatched[i]);
        console.log(result);
    }
     else if(perMatched[i] === toString){
        console.log("hey i found it....i added it to the results array");
        result.push(perMatched[i]);
        console.log(result);
    }    
}
}
perMatched(name1,name2);